// src/Theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ffb300', // Amber color representing honey
    },
    secondary: {
      main: '#4caf50', // Green color
    },
  },
});

export default theme;